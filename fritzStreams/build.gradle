apply plugin: 'android'

android {
    compileSdkVersion 17
    buildToolsVersion '20'

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }

    defaultConfig {
        applicationId "de.acepe.fritzstreams"
        minSdkVersion 15
        targetSdkVersion 20

        ndk {
            moduleName "dump"
            cFlags "-D__STDC_CONSTANT_MACROS -DNO_CRYPTO -DRTMPDUMP_VERSION=\\\"v2.4\\\""
            ldLibs "log"
        }
    }

    buildTypes {
        release {
            runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
    sourceSets {
        main {
            jniLibs.srcDirs = ['jniLibs']
            // jni.srcDirs = [] //disable automatic ndk-build
        }
    }

}
dependencies {
    compile project(':androidffmpeglibrary')
    compile project(':RtmpDump')
    compile project(':android-support-v4-preferencefragment')
    compile 'com.android.support:appcompat-v7:20.+'
}
